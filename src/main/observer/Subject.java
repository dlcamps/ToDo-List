/*
package observer;

import java.util.ArrayList;
import java.util.List;

public class Subject {
    private List<ListObserver> observers = new ArrayList<>();

    public void addObserver(ListObserver listObserver) {
        if (!observers.contains(listObserver)) {
            observers.add(listObserver);
        }
    }
}
*/
